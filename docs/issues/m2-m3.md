# Issue pack: M2 + M3

Drafts for `gh issue create`. Each block has a title, a label suggestion, and a body that maps to the work tracked in [ROADMAP.md](../../ROADMAP.md).

To file all of them in one shot, use [`file_issues.sh`](file_issues.sh).

> **Note (a bunch of these shipped).** Vision and mission-control interfaces + mocks landed (ADR-0003). Per-command deny list landed (ADR-0004). World state landed (ADR-0005). The original issue #6 (`mock_follow_task`) shipped as `examples/local_sim_demo.py`. Open issues now: PiHardwareController, real YoloVision adapter, real GemmaMissionControl adapter.

---

## 1. ~~Per-command allow/deny in Config + Router~~ — **shipped**

**Status:** done. `Config.denied_commands` (env: `FREEMOTION_DENIED_COMMANDS`) is enforced in `Router.dispatch`; refused commands return `error.code = "denied_by_policy"`; `stop` is exempt. See [ADR-0004](../decisions.md#adr-0004--per-command-allowdeny-allow-by-default-explicit-deny-list-stop-always-exempt--2026-05-03).

---

## 2. PiHardwareController in `freemotion/hardware/`

**Labels:** `enhancement`, `m2`, `hardware`

**Body:**

`MockHardwareController` proves the contract; now ship a real `PiHardwareController` so the Pi can be a first-class device beyond peripherals.

Scope:

- New class implementing the `HardwareController` Protocol.
- Stub `arm` / `disarm` / `move` until the project picks a motor stack (PWM hat, MAVLink to a flight controller, etc.).
- The first stub can simply log + reply, with a clear TODO and ADR pointer.
- Migration: `examples/pipe_check/` adopts the new controller for `stop` and `status`; the LED handlers stay example-local.

Acceptance:

- `tests/test_pi_hardware.py` (mocking `RPi.GPIO`).
- `docs/pi-runtime.md` updated to describe the controller-vs-peripheral distinction.
- Separate ADR for the motor-stack pick (deferred — not part of this issue).

---

## 3. YoloVision adapter behind a feature flag

**Labels:** `enhancement`, `m3`, `vision`

**Body:**

The `VisionBackend` interface and `MockVision` shipped (see [`docs/models.md`](../models.md), ADR-0003). Now ship the first real adapter without making the dependency mandatory.

Scope:

- `freemotion/vision/yolo.py` exposing `YoloVision` (implements `VisionBackend`).
- Wraps `ultralytics` (or equivalent); imports lazily so the package still loads on machines without the dep.
- v1 detections limited to `person` and a small set of obstacle classes; everything else filtered out.
- New env var `FREEMOTION_VISION_BACKEND=mock|yolo` (default `mock`). Reads in the example wiring, not in the interface.
- `pyproject.toml` gets an optional extra: `pip install -e .[yolo]` pulls the deps.

Acceptance:

- `tests/test_yolo_vision.py` skips cleanly when `ultralytics` isn't installed; runs end-to-end on a tiny fixture image when it is.
- `docs/models.md` updated with the install + run command.
- ADR for any non-obvious call (e.g. model size, frame size, NMS threshold).

---

## 4. GemmaMissionControl adapter behind a feature flag

**Labels:** `enhancement`, `m3`, `mission`

**Body:**

The `MissionPolicy` interface and `MockMissionControl` shipped (see [`docs/models.md`](../models.md), ADR-0003). Now ship the first real adapter without making the dependency mandatory.

Scope:

- `freemotion/mission_control/gemma.py` exposing `GemmaMissionControl` (implements `MissionPolicy`).
- Wraps `transformers` or `llama.cpp`; imports lazily.
- v1 outputs are constrained: parse intent → emit one `CommandName` + args + short reason + confidence. **Not** free-form robotics.
- New env var `FREEMOTION_MISSION_BACKEND=mock|gemma` (default `mock`).
- `pyproject.toml` gets an optional extra: `pip install -e .[gemma]`.

Acceptance:

- `tests/test_gemma_mission_control.py` skips cleanly when the dep isn't installed; runs offline against a tiny fixture model when it is.
- `docs/models.md` updated.
- ADR for the structured-output strategy (function-call format, regex constrained decoding, etc.).

---

## 5. ~~Shared world state: `freemotion.world`~~ — **shipped**

**Status:** done. `freemotion/world/` ships `WorldStateSnapshot` (frozen) and `WorldState` (lock-protected wrapper). `MissionPolicy.plan` now takes `WorldStateSnapshot` directly. The demo passes a live snapshot through every tick. See [ADR-0005](../decisions.md#adr-0005--world-state-v1-narrow-lock-protected-snapshot-shaped--2026-05-03).

Remaining minor work (folds into a small follow-up PR rather than a fresh issue):

- Wire `WorldState` into the `/status` reply telemetry so the device exposes its current target / next_action over the protocol. Today the demo prints it; the `/status` handler still doesn't see it.

---

## 6. ~~examples/mock_follow_task~~ — **shipped as `examples/local_sim_demo.py`**

**Status:** done. The script lives at [`examples/local_sim_demo.py`](../../examples/local_sim_demo.py); the long-form write-up lives at [`docs/demo.md`](../demo.md). Smoke test: `tests/test_local_sim_demo.py`.

What's left from the original scope after world state lands (issue 5):

- Wire the `world={}` arg in the demo to a real `WorldState` instance.
- Add a `/status` reply that reflects mission + vision + hardware state in one telemetry blob.

Both fold cleanly into issue 5 once it ships; no separate issue needed.

---

## How to file these

Each block above maps cleanly to a GitHub issue body. Three ways to file them.

### Manual

Click "New issue" on the repo, paste a title from the headers, paste the body, add the labels listed above. Six issues in five minutes.

### One command

```bash
bash docs/issues/file_issues.sh
```

The script files all six. Comment out blocks you don't want.

### Scripted snippet

```bash
gh issue create \
  --title "Per-command allow/deny in Config + Router" \
  --label "enhancement,m2,runtime" \
  --body-file - <<'EOF'
The Router currently dispatches any registered command to its handler. ...
EOF
```

If a label doesn't exist yet, create it once with `gh label create <name>`.

### Recording filed issues

Once filed, link the issue numbers next to the corresponding bullets in [ROADMAP.md](../../ROADMAP.md) so a contributor can jump from "what's planned" to "where the work happens" in one click.
