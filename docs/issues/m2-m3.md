# Issue pack: M2 + M3

Drafts for `gh issue create`. Each block has a title, a label suggestion, and a body that maps to the work tracked in [ROADMAP.md](../../ROADMAP.md).

These are **not** filed yet. Paste each block into GitHub manually, or use the `gh` snippets at the bottom.

---

## 1. Per-command allow/deny in Config + Router

**Labels:** `enhancement`, `m2`, `runtime`

**Body:**

The Router currently dispatches any registered command to its handler. We need an opt-in deny list per device, so a Pi configured for "vision only" can refuse `arm` / `move` even if the handler is wired.

Scope:

- `Config.denied_commands: FrozenSet[str]` (parsed from `FREEMOTION_DENIED_COMMANDS`).
- `Router.dispatch` checks the deny set before invoking the handler; refused commands return `error.code = "denied_by_policy"` (new code) or `unauthorized`.
- One ADR entry recording the call between "deny by default" vs "allow by default" (proposed: allow by default, with an explicit deny list as policy).
- Tests covering allow path, deny path, and the `stop` exception (`stop` is honored regardless of policy).

Acceptance:

- New env var documented in `docs/pi-runtime.md`.
- `examples/pipe_check/README.md` and `examples/mock_drone/README.md` note the option.

---

## 2. PiHardwareController in `freemotion/hardware/`

**Labels:** `enhancement`, `m2`, `hardware`

**Body:**

`MockHardwareController` proves the contract; now ship a real `PiHardwareController` so the Pi can be a first-class device beyond peripherals.

Scope:

- New class implementing the `HardwareController` Protocol.
- Stub `arm` / `disarm` / `move` until the project picks a motor stack (PWM hat, MAVLink to a flight controller, etc.).
- The first stub can simply log + reply, with a clear TODO and ADR pointer.
- Migration: `examples/pipe_check/` adopts the new controller for `stop` and `status`; the LED handlers stay example-local.

Acceptance:

- `tests/test_pi_hardware.py` (mocking `RPi.GPIO`).
- `docs/pi-runtime.md` updated to describe the controller-vs-peripheral distinction.
- Separate ADR for the motor-stack pick (deferred — not part of this issue).

---

## 3. Vision interface stub: `freemotion.vision`

**Labels:** `enhancement`, `m3`, `vision`

**Body:**

Lay the interface that YOLO will plug into. No real model yet.

Scope:

- `freemotion/vision/__init__.py` exporting a `VisionBackend` Protocol.
- `freemotion/vision/mock.py` returning canned detections.
- Methods: `detect_person()`, `detect_obstacles()`, `scene()`.
- Wired into `Config.enabled_features` (e.g. `FREEMOTION_FEATURES=vision`) and exposed via a `/scene` command behind the feature flag.

Acceptance:

- Tests for `MockVisionBackend`.
- `docs/models.md` (new) describing the interface and the swap path to YOLO.

---

## 4. Mission control interface stub: `freemotion.mission_control`

**Labels:** `enhancement`, `m3`, `mission`

**Body:**

Lay the interface that Gemma small will plug into.

Scope:

- `freemotion/mission_control/__init__.py` exporting a `MissionPolicy` Protocol.
- `freemotion/mission_control/rule_based.py` as the first non-LLM impl.
- Methods: `parse_intent(text) -> Goal`, `choose_action(goal, scene, state) -> Action`, `next_step(state) -> Action`.
- Reads world state (issue #5) and emits actions matching the protocol's command set.

Acceptance:

- Tests for the rule-based policy.
- `docs/models.md` updated.

---

## 5. Shared world state: `freemotion.world`

**Labels:** `enhancement`, `m3`, `state`

**Body:**

A small, structured place for "what does the device think is true right now."

Fields:

- `target` (current goal)
- `current_state` (idle / armed / moving / ...)
- `confidence` (0..1)
- `last_seen` (per target)
- `next_action`

Scope:

- `freemotion/world/__init__.py` with a `WorldState` dataclass and a thread-safe accessor.
- Updated by vision and mission_control, read by the router for `/status`.

Acceptance:

- Tests for concurrent updates.
- Wired into one of the `/status` reply telemetry fields.

---

## 6. examples/mock_follow_task

**Labels:** `example`, `m3`

**Body:**

A second runnable example that closes the loop:

1. `MockVisionBackend` "sees" a target.
2. `MissionPolicy` decides to follow.
3. `MockHardwareController` reports a fake position update.
4. World state and `/status` reflect the loop.

No real hardware. No real models. Pure demonstration of the M3 loop.

Acceptance:

- `examples/mock_follow_task/README.md` with run command.
- One five-step demo script anyone can paste into a Telegram chat.

---

## How to file these

Each block above maps cleanly to a GitHub issue body. Two ways to file them.

### Manual (recommended for first pass)

Click "New issue" on the repo, paste a title from the headers, paste the body, add the labels listed above. Six issues in five minutes.

### Scripted (`gh`)

```bash
# from the repo root
gh issue create \
  --title "Per-command allow/deny in Config + Router" \
  --label "enhancement" --label "m2" --label "runtime" \
  --body-file - <<'EOF'
The Router currently dispatches any registered command to its handler. ...
EOF
```

Repeat per issue. The `--body-file -` heredoc form keeps formatting clean. If a label doesn't exist yet, create it first with `gh label create <name>`.

### Recording filed issues

Once filed, link the issue numbers next to the corresponding bullets in [ROADMAP.md](../../ROADMAP.md) so a contributor can jump from "what's planned" to "where the work happens" in one click.
